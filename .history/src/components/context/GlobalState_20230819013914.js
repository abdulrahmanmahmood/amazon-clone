import { createContext } from "react";

const Globalcontext = createContext();

const GlobalProvider = () => {
  return <Globalcontext.Provider va>

  </Globalcontext.Provider>;
};

export default GlobalProvider;
